# ── LOCAL / NO-DOCKER requirements (lightweight) ──────────────────────────
# This is a trimmed set for running on a laptop WITHOUT Docker, Postgres, or
# Redis, and WITHOUT the heavy ML stack (PyTorch / FAISS / sentence-transformers).
#
#   - DB:        SQLite (built into the app's fallback) — no Postgres needed
#   - Queue:     jobs run inline — no Redis server needed
#   - Retrieval: BM25-only (set EMBEDDING_BACKEND=none) — no torch/faiss
#
# Install with:  pip install -r requirements-local.txt
# For the full vector-RAG stack instead, use requirements.txt.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Config / validation
pydantic==2.7.1
pydantic-settings==2.3.4
python-dotenv==1.0.1

# LLM (Groq — OpenAI-compatible function calling)
groq>=0.11.0
openai>=1.60.0

# Database — SQLite for instant local runs, asyncpg for Neon/Postgres
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0

# Queue client (import-only; no Redis server required — falls back to inline).
# arq pulls a compatible redis client itself, so we don't pin redis separately.
arq==0.26.0

# Document parsing (text PDFs work; OCR for scanned images is optional)
pdfplumber==0.11.1
Pillow==10.3.0

# PDF export
fpdf2==2.7.9

# Utilities
httpx==0.27.0
numpy==1.26.4
